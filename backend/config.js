// Define the PORT for the application to listen on. This is set to 8080 by default.
export const PORT = 8080; 

// Define the MongoDB connection URL. 
// This is the URL where your MongoDB database is hosted. 
// 'mongodb://127.0.0.1:27017/BookStore' connects to a local MongoDB instance and the 'BookStore' database.
export const MONGO_URL = "mongodb://127.0.0.1:27017/BookStore"; 

// If you want to use a different database, simply replace 'BookStore' with the name of your new database, 
// for example: 
// export const MONGO_URL = "mongodb://127.0.0.1:27017/YourNewDatabaseName"; 
